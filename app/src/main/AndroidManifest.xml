<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/truckalert"
        android:label="Truck Alert"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TruckAlert"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Main App Screen -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"
            />

        <!-- Splash Screen (entry point) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Camera & Sensors Screen -->
        <activity
            android:name=".CameraSensorsActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"
            />

        <!-- Profile Screen -->
        <activity
            android:name=".RecordingActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"
            />

        <!-- Fullscreen Camera Screen -->
        <activity
            android:name=".FullScreenCameraActivity"
            android:screenOrientation="fullSensor"
            android:exported="false" />

        <!-- Recording Player Screen -->
        <activity
            android:name=".RecordingPlayerActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

    </application>

</manifest>
